# Python dependencies for PHIreason pipeline
# Install with: pip install -r requirements.txt

# Async HTTP client — used by run_inference.py for Ollama API calls
aiohttp>=3.9

# Numerical computation — metrics calculation in run_inference.py
numpy>=1.24

# Evaluation metrics — AUC, AUPR in run_inference.py
scikit-learn>=1.3
